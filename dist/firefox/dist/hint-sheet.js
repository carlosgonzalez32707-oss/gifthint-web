(() => {
  // ../../../../private/var/folders/gf/cg_yq02x5zs0tn6wdvydnplm0000gn/T/tmp.iU2yKg31WA/hint-sheet.js
  var DNA_TAG_LIBRARY = {
    clothing: {
      label: "Clothing / Fashion",
      emoji: "\u{1F457}",
      tags: [
        "#NoSynthetics",
        "#NoPink",
        "#NoLogoVisible",
        "#SizeUp",
        "#SizeDown",
        "#NaturalFabric",
        "#NoPatterns",
        "#DarkColours",
        "#LightColours",
        "#NoBranding"
      ],
      detectionTerms: [
        "shirt",
        "tshirt",
        "t-shirt",
        "dress",
        "jacket",
        "coat",
        "hoodie",
        "sweater",
        "jeans",
        "pants",
        "trousers",
        "skirt",
        "blouse",
        "top",
        "vest",
        "cardigan",
        "jumper",
        "pullover",
        "shorts",
        "leggings",
        "pyjama",
        "pajama",
        "underwear",
        "socks",
        "scarf",
        "gloves",
        "hat",
        "cap",
        "beanie",
        "clothing",
        "apparel",
        "fashion",
        "outfit",
        "wear",
        "fabric",
        "cotton",
        "linen",
        "silk",
        "wool",
        "denim",
        "nordstrom",
        "zara",
        "h&m",
        "gap",
        "uniqlo",
        "asos",
        "primark"
      ]
    },
    electronics: {
      label: "Electronics",
      emoji: "\u{1F50C}",
      tags: [
        "#NoWhite",
        "#NoChrome",
        "#WiredOnly",
        "#WirelessOnly",
        "#NoBluetooth",
        "#NoApple",
        "#AndroidOnly",
        "#iOSOnly",
        "#NoTouchscreen",
        "#LongBatteryLife"
      ],
      detectionTerms: [
        "headphone",
        "earphone",
        "earbud",
        "speaker",
        "keyboard",
        "mouse",
        "laptop",
        "tablet",
        "phone",
        "charger",
        "cable",
        "monitor",
        "screen",
        "camera",
        "tv",
        "television",
        "gaming",
        "controller",
        "console",
        "watch",
        "smartwatch",
        "tracker",
        "gadget",
        "device",
        "electronics",
        "wireless",
        "bluetooth",
        "usb",
        "hdmi",
        "audio",
        "microphone",
        "webcam",
        "apple",
        "samsung",
        "sony",
        "bose",
        "jabra",
        "logitech",
        "anker"
      ]
    },
    books: {
      label: "Books",
      emoji: "\u{1F4DA}",
      tags: [
        "#HardcoverOnly",
        "#PaperbackOK",
        "#NoDigital",
        "#FirstEdition",
        "#IllustratedEdition",
        "#LargePrint",
        "#AudiobookOK"
      ],
      detectionTerms: [
        "book",
        "novel",
        "memoir",
        "biography",
        "hardcover",
        "paperback",
        "audiobook",
        "ebook",
        "edition",
        "volume",
        "series",
        "fiction",
        "nonfiction",
        "non-fiction",
        "thriller",
        "romance",
        "mystery",
        "cookbook",
        "textbook",
        "anthology",
        "poetry",
        "graphic novel",
        "amazon books",
        "bookshop",
        "waterstones",
        "barnes",
        "noble"
      ]
    },
    beauty: {
      label: "Beauty / Skincare",
      emoji: "\u2728",
      tags: [
        "#NoRetinol",
        "#FragranceFree",
        "#CrueltyFree",
        "#NoParabens",
        "#VeganFormula",
        "#SPFRequired",
        "#NoAlcohol",
        "#HypoallergenicOnly",
        "#NaturalIngredients"
      ],
      detectionTerms: [
        "skincare",
        "moisturiser",
        "moisturizer",
        "serum",
        "toner",
        "cleanser",
        "sunscreen",
        "spf",
        "retinol",
        "vitamin c",
        "hyaluronic",
        "makeup",
        "foundation",
        "lipstick",
        "mascara",
        "eyeshadow",
        "blush",
        "concealer",
        "perfume",
        "fragrance",
        "cologne",
        "lotion",
        "cream",
        "oil",
        "balm",
        "shampoo",
        "conditioner",
        "mask",
        "scrub",
        "exfoliant",
        "tanning",
        "sephora",
        "ulta",
        "lookfantastic",
        "boots",
        "superdrug",
        "cerave",
        "the ordinary",
        "la roche",
        "neutrogena",
        "olay",
        "clinique"
      ]
    },
    shoes: {
      label: "Shoes",
      emoji: "\u{1F45F}",
      tags: [
        "#NarrowFit",
        "#WideFit",
        "#NaturalSole",
        "#NoHeels",
        "#HeelsOnly",
        "#HalfSizeUp",
        "#HalfSizeDown",
        "#VeganLeather",
        "#NoLeather"
      ],
      detectionTerms: [
        "shoe",
        "boot",
        "sneaker",
        "trainer",
        "sandal",
        "heel",
        "flat",
        "loafer",
        "mule",
        "slipper",
        "pump",
        "oxford",
        "derby",
        "chelsea",
        "running shoe",
        "walking shoe",
        "hiking boot",
        "ankle boot",
        "nike",
        "adidas",
        "new balance",
        "vans",
        "converse",
        "reebok",
        "puma",
        "skechers",
        "birkenstock",
        "ugg",
        "dr martens",
        "clarks"
      ]
    },
    home: {
      label: "Home / Decor",
      emoji: "\u{1F3E0}",
      tags: [
        "#MinimalistStyle",
        "#NoBold",
        "#NeutralColours",
        "#VintageStyle",
        "#ModernOnly",
        "#NoPlastic",
        "#WoodOnly",
        "#HandmadePreferred",
        "#NoAssemblyRequired",
        "#EcoFriendly"
      ],
      detectionTerms: [
        "candle",
        "vase",
        "mug",
        "cup",
        "bowl",
        "plate",
        "lamp",
        "light",
        "cushion",
        "pillow",
        "throw",
        "blanket",
        "rug",
        "frame",
        "print",
        "poster",
        "plant",
        "pot",
        "organiser",
        "organizer",
        "shelf",
        "storage",
        "towel",
        "bedding",
        "duvet",
        "sheet",
        "furniture",
        "chair",
        "table",
        "desk",
        "sofa",
        "couch",
        "decor",
        "decoration",
        "ornament",
        "figurine",
        "kitchen",
        "bathroom",
        "bedroom",
        "living room",
        "garden",
        "outdoor",
        "ikea",
        "west elm",
        "made.com",
        "anthropologie",
        "pottery barn",
        "home depot",
        "wayfair",
        "dunelm",
        "habitat",
        "john lewis"
      ]
    },
    generic: {
      label: "General",
      emoji: "\u{1F381}",
      tags: [
        "#GiftReceiptPlease",
        "#NoAssemblyRequired",
        "#EcoFriendly",
        "#MadeLocally",
        "#FairTrade",
        "#NoPlastic",
        "#RecycledMaterials",
        "#CompactSize",
        "#LargerSizePreferred"
      ],
      detectionTerms: []
    }
  };
  var ALL_DNA_TAGS = (() => {
    const seen = /* @__PURE__ */ new Set();
    const out = [];
    for (const cat of Object.values(DNA_TAG_LIBRARY)) {
      for (const tag of cat.tags) {
        if (!seen.has(tag)) {
          seen.add(tag);
          out.push(tag);
        }
      }
    }
    return out;
  })();
  function validateTag(tag) {
    return typeof tag === "string" && /^#[A-Za-z0-9]{1,19}$/.test(tag);
  }
  function suggestTagsForItem(title, retailer) {
    const haystack = `${title} ${retailer}`.toLowerCase();
    const matches = [];
    for (const [key, cat] of Object.entries(DNA_TAG_LIBRARY)) {
      if (key === "generic") continue;
      let score = 0;
      for (const term of cat.detectionTerms) {
        if (haystack.includes(term)) score += term.length;
      }
      if (score > 0) matches.push({ key, score });
    }
    matches.sort((a, b) => b.score - a.score);
    const keys = matches.map((m) => m.key);
    if (!keys.includes("generic")) keys.push("generic");
    const seen = /* @__PURE__ */ new Set();
    const out = [];
    for (const key of keys.slice(0, 3)) {
      for (const tag of DNA_TAG_LIBRARY[key].tags) {
        if (!seen.has(tag) && out.length < 12) {
          seen.add(tag);
          out.push(tag);
        }
      }
    }
    return out;
  }
  function searchTags(query, maxResults = 8) {
    const q = query.replace(/^#/, "").toLowerCase().trim();
    if (!q) return ALL_DNA_TAGS.slice(0, maxResults);
    const prefix = [], sub = [];
    for (const tag of ALL_DNA_TAGS) {
      const body = tag.slice(1).toLowerCase();
      if (body.startsWith(q)) prefix.push(tag);
      else if (body.includes(q)) sub.push(tag);
    }
    return [...prefix, ...sub].slice(0, maxResults);
  }
  var popularTagCache = null;
  async function fetchPopularTags() {
    if (popularTagCache) return popularTagCache;
    try {
      const res = await fetch("https://gifthint.io/api/dna-tags/popular?limit=50");
      const json = await res.json();
      popularTagCache = new Map((json.tags ?? []).map((t) => [t.tag_text, t.usage_count]));
    } catch {
      popularTagCache = /* @__PURE__ */ new Map();
    }
    return popularTagCache;
  }
  var sheetState = null;
  var PURPLE = "#8B83F0";
  var SURFACE2 = "#1C1C22";
  var BORDER = "rgba(240,238,232,0.08)";
  var TEXT = "#F0EEE8";
  var MUTED = "#7A7870";
  function renderSheet() {
    const { hint, tags } = sheetState;
    const suggestedAll = suggestTagsForItem(sheetState.itemTitle, sheetState.itemRetailer);
    const suggestions = suggestedAll.filter((t) => !tags.includes(t));
    const popular = popularTagCache ?? /* @__PURE__ */ new Map();
    const tagChips = tags.map((t) => `
    <span class="gh-chip" data-tag="${t}" style="
      display:inline-flex;align-items:center;gap:3px;
      font-size:10px;font-weight:700;padding:2px 5px 2px 8px;
      border-radius:999px;background:${PURPLE}22;border:1px solid ${PURPLE}44;
      color:${PURPLE};white-space:nowrap;line-height:1.6;
    ">
      ${t}
      <button class="gh-chip-remove" data-tag="${t}" style="
        background:none;border:none;cursor:pointer;color:${PURPLE};
        opacity:0.65;padding:0;line-height:1;font-size:13px;
      ">\xD7</button>
    </span>
  `).join("");
    const suggPills = suggestions.map((t) => {
      const count = popular.get(t) ?? 0;
      const badge = count > 4 ? `<span style="font-size:9px;background:${PURPLE}33;color:${PURPLE};
                      border-radius:999px;padding:1px 4px;font-weight:700;">${count > 99 ? "99+" : count}</span>` : "";
      return `
      <button class="gh-sugg" data-tag="${t}" style="
        display:inline-flex;align-items:center;gap:3px;
        font-size:10px;font-weight:600;padding:2px 7px;
        border-radius:999px;background:${SURFACE2};border:1px solid ${BORDER};
        color:${TEXT};cursor:pointer;white-space:nowrap;line-height:1.6;
      ">${t}${badge}</button>
    `;
    }).join("");
    document.getElementById("gh-hint-sheet").innerHTML = `
    <div style="
      position:fixed;bottom:0;left:0;right:0;
      background:#141418;border-top:1px solid ${BORDER};
      border-radius:16px 16px 0 0;padding:20px 18px 24px;
      font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;
      box-shadow:0 -8px 32px rgba(0,0,0,0.4);
      max-height:80%;overflow-y:auto;
    " id="gh-sheet-inner">

      <!-- Handle -->
      <div style="width:36px;height:4px;background:${BORDER};border-radius:2px;
                  margin:0 auto 16px;"></div>

      <h3 style="margin:0 0 14px;font-size:13px;font-weight:800;color:${TEXT};
                 letter-spacing:-0.02em;">\u270F\uFE0F Hint &amp; preferences</h3>

      <!-- Hint textarea -->
      <label style="display:block;font-size:10.5px;font-weight:600;color:${MUTED};
                    text-transform:uppercase;letter-spacing:0.04em;margin-bottom:5px;">
        Gifter hint
      </label>
      <textarea id="gh-hint-input" rows="2" maxlength="500"
        placeholder="Size, colour, or any note for your gifters\u2026"
        style="
          width:100%;box-sizing:border-box;padding:8px 10px;
          border-radius:8px;border:1px solid ${BORDER};
          background:${SURFACE2};color:${TEXT};font-size:12px;
          resize:vertical;outline:none;font-family:inherit;
          line-height:1.5;
        ">${escapeHtml(hint ?? "")}</textarea>

      <!-- Tag editor -->
      <label style="display:block;font-size:10.5px;font-weight:600;color:${MUTED};
                    text-transform:uppercase;letter-spacing:0.04em;
                    margin:14px 0 5px;">
        \u{1F9EC} Preference tags
      </label>

      <!-- Chip strip + input -->
      <div id="gh-tag-field" style="
        display:flex;flex-wrap:wrap;align-items:center;gap:5px;
        padding:7px 10px;border-radius:8px;border:1px solid ${BORDER};
        background:${SURFACE2};min-height:38px;cursor:text;
      ">
        ${tagChips}
        <input id="gh-tag-input" type="text" placeholder="#\u2026"
          style="flex:1 1 60px;min-width:50px;background:transparent;border:none;
                 outline:none;color:${TEXT};font-size:12px;padding:2px 0;
                 font-family:inherit;"
          aria-label="Add a preference tag" />
      </div>
      <div id="gh-tag-error" style="font-size:11px;color:#E24B4A;margin-top:4px;
                                    display:none;"></div>

      <!-- Autocomplete dropdown -->
      <div id="gh-tag-dropdown" style="display:none;margin-top:4px;
        border-radius:8px;border:1px solid ${BORDER};background:#1A1A20;
        overflow:hidden;box-shadow:0 4px 16px rgba(0,0,0,0.3);"></div>

      <!-- Suggestions -->
      ${suggestions.length > 0 ? `
        <p style="margin:10px 0 5px;font-size:10.5px;color:${MUTED};">
          Suggested for this item:
        </p>
        <div id="gh-suggestions" style="display:flex;flex-wrap:wrap;gap:4px;">
          ${suggPills}
        </div>
      ` : ""}

      <!-- Save error banner (hidden by default) -->
      <div id="gh-save-error" style="
        display:none;margin-top:12px;padding:8px 10px;
        border-radius:8px;border:1px solid rgba(226,75,74,0.3);
        background:rgba(226,75,74,0.1);color:#E24B4A;font-size:11.5px;
        line-height:1.45;
      "></div>

      <!-- Actions -->
      <div style="display:flex;gap:8px;margin-top:16px;">
        <button id="gh-sheet-save" style="
          flex:1;padding:10px;border-radius:8px;border:none;
          background:${PURPLE};color:#fff;font-size:13px;font-weight:700;
          cursor:pointer;letter-spacing:-0.01em;
        ">Save</button>
        <button id="gh-sheet-cancel" style="
          padding:10px 14px;border-radius:8px;background:transparent;
          border:1px solid ${BORDER};color:${MUTED};font-size:13px;cursor:pointer;
        ">Cancel</button>
      </div>

    </div>
  `;
    bindSheetEvents();
  }
  function escapeHtml(str) {
    return str.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
  }
  var dropdownItems = [];
  var dropdownIdx = -1;
  function bindSheetEvents() {
    const tagInput = document.getElementById("gh-tag-input");
    const dropdown = document.getElementById("gh-tag-dropdown");
    const errDiv = document.getElementById("gh-tag-error");
    const tagField = document.getElementById("gh-tag-field");
    tagField.addEventListener("click", (e) => {
      if (e.target === tagField) tagInput.focus();
    });
    document.querySelectorAll(".gh-chip-remove").forEach((btn) => {
      btn.addEventListener("click", () => {
        removeTag(btn.dataset.tag);
      });
    });
    document.querySelectorAll(".gh-sugg").forEach((btn) => {
      btn.addEventListener("click", () => addTag(btn.dataset.tag));
    });
    tagInput.addEventListener("input", () => {
      const q = tagInput.value.trim();
      if (!q) {
        hideDropdown();
        return;
      }
      dropdownItems = searchTags(q, 8).filter((t) => !sheetState.tags.includes(t));
      dropdownIdx = -1;
      renderDropdown();
    });
    tagInput.addEventListener("keydown", (e) => {
      if (e.key === "ArrowDown") {
        e.preventDefault();
        moveDrop(1);
      } else if (e.key === "ArrowUp") {
        e.preventDefault();
        moveDrop(-1);
      } else if (e.key === "Enter") {
        e.preventDefault();
        if (dropdownIdx >= 0 && dropdownItems[dropdownIdx]) {
          addTag(dropdownItems[dropdownIdx]);
        } else if (tagInput.value.trim()) {
          addTag(tagInput.value);
        }
      } else if (e.key === "Escape") hideDropdown();
      else if (e.key === "Backspace" && !tagInput.value && sheetState.tags.length > 0) {
        removeTag(sheetState.tags[sheetState.tags.length - 1]);
      }
    });
    document.addEventListener("mousedown", (e) => {
      if (!dropdown.contains(e.target) && e.target !== tagInput) hideDropdown();
    }, { once: false });
    document.getElementById("gh-sheet-save").addEventListener("click", saveSheet);
    document.getElementById("gh-sheet-cancel").addEventListener("click", closeSheet);
  }
  function renderDropdown() {
    const dd = document.getElementById("gh-tag-dropdown");
    const popular = popularTagCache ?? /* @__PURE__ */ new Map();
    if (dropdownItems.length === 0) {
      dd.style.display = "none";
      return;
    }
    dd.style.display = "block";
    dd.innerHTML = dropdownItems.map((tag, i) => {
      const count = popular.get(tag) ?? 0;
      const badge = count > 0 ? `<span style="font-size:10px;color:${MUTED};background:#1C1C28;
                      border-radius:999px;padding:1px 6px;">${count} uses</span>` : "";
      return `
      <button class="gh-drop-item" data-tag="${tag}" style="
        display:flex;align-items:center;justify-content:space-between;
        width:100%;padding:8px 12px;background:${i === dropdownIdx ? SURFACE2 : "transparent"};
        border:none;cursor:pointer;text-align:left;font-size:12px;
        color:${TEXT};font-family:inherit;
      ">
        <span style="font-weight:600;">${tag}</span>${badge}
      </button>
    `;
    }).join("");
    dd.querySelectorAll(".gh-drop-item").forEach((btn) => {
      btn.addEventListener("mousedown", (e) => {
        e.preventDefault();
        addTag(btn.dataset.tag);
      });
    });
  }
  function moveDrop(dir) {
    dropdownIdx = Math.max(-1, Math.min(dropdownIdx + dir, dropdownItems.length - 1));
    renderDropdown();
  }
  function hideDropdown() {
    dropdownItems = [];
    dropdownIdx = -1;
    const dd = document.getElementById("gh-tag-dropdown");
    if (dd) dd.style.display = "none";
  }
  function showError(msg) {
    const el = document.getElementById("gh-tag-error");
    if (!el) return;
    el.textContent = msg;
    el.style.display = "block";
    setTimeout(() => {
      if (el) el.style.display = "none";
    }, 3e3);
  }
  function addTag(raw) {
    const tag = raw.trim().startsWith("#") ? raw.trim() : `#${raw.trim()}`;
    if (!validateTag(tag)) {
      showError("Tags must start with # and use only letters/numbers (max 20 chars).");
      return;
    }
    if (sheetState.tags.includes(tag)) {
      showError(`${tag} is already added.`);
      return;
    }
    if (sheetState.tags.length >= 10) {
      showError("Maximum 10 tags per item.");
      return;
    }
    sheetState.tags = [...sheetState.tags, tag];
    const input = document.getElementById("gh-tag-input");
    if (input) input.value = "";
    hideDropdown();
    renderSheet();
  }
  function removeTag(tag) {
    sheetState.tags = sheetState.tags.filter((t) => t !== tag);
    renderSheet();
  }
  async function saveSheet() {
    const hint = (document.getElementById("gh-hint-input")?.value ?? "").trim() || null;
    const tags = sheetState.tags;
    const saveBtn = document.getElementById("gh-sheet-save");
    if (saveBtn) {
      saveBtn.disabled = true;
      saveBtn.textContent = "Saving\u2026";
    }
    try {
      const { accessToken } = await new Promise(
        (resolve) => chrome.storage.local.get(["accessToken"], resolve)
      );
      const res = await fetch(`https://gifthint.io/api/items/${sheetState.itemId}`, {
        method: "PATCH",
        headers: {
          "Content-Type": "application/json",
          ...accessToken ? { Authorization: `Bearer ${accessToken}` } : {}
        },
        body: JSON.stringify({ hint, dna_tags: tags })
      });
      if (!res.ok) throw new Error(`HTTP ${res.status}`);
      const result = { hint, tags };
      sheetState.resolve(result);
      renderSuccessScreen(hint, tags);
    } catch (err) {
      console.error("[GiftHint/hint-sheet] save failed:", err);
      if (saveBtn) {
        saveBtn.disabled = false;
        saveBtn.textContent = "Save";
      }
      const errBanner = document.getElementById("gh-save-error");
      if (errBanner) {
        errBanner.textContent = `Save failed (${err.message}). Please try again.`;
        errBanner.style.display = "block";
        setTimeout(() => {
          if (errBanner) errBanner.style.display = "none";
        }, 4e3);
      }
      sheetState.reject(err);
    }
  }
  function renderSuccessScreen(hint, tags) {
    const container = document.getElementById("gh-hint-sheet");
    if (!container) return;
    const { itemId, wishlistSlug } = sheetState;
    const dashboardUrl = wishlistSlug ? `https://gifthint.io/dashboard/${wishlistSlug}?edit=${itemId}` : `https://gifthint.io/dashboard?editItem=${itemId}`;
    const tagSummary = tags.length > 0 ? `<p style="margin:6px 0 0;font-size:10.5px;color:${MUTED};">
         Tags saved: ${tags.map((t) => `<span style="color:${PURPLE};font-weight:600;">${escapeHtml(t)}</span>`).join(", ")}
       </p>` : "";
    container.innerHTML = `
    <div style="
      position:fixed;bottom:0;left:0;right:0;
      background:#141418;border-top:1px solid ${BORDER};
      border-radius:16px 16px 0 0;padding:24px 18px 32px;
      font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;
      box-shadow:0 -8px 32px rgba(0,0,0,0.4);
      text-align:center;
    ">
      <!-- Handle -->
      <div style="width:36px;height:4px;background:${BORDER};border-radius:2px;
                  margin:0 auto 20px;"></div>

      <!-- Success icon -->
      <div style="
        width:48px;height:48px;border-radius:50%;
        background:rgba(78,201,154,0.15);border:1px solid rgba(78,201,154,0.35);
        display:flex;align-items:center;justify-content:center;
        margin:0 auto 14px;font-size:22px;
      ">\u2713</div>

      <h3 style="margin:0 0 6px;font-size:15px;font-weight:800;color:#F0EEE8;
                 letter-spacing:-0.02em;">Saved!</h3>
      <p style="margin:0;font-size:12px;color:${MUTED};line-height:1.5;">
        ${hint ? `"${escapeHtml(hint.slice(0, 60))}${hint.length > 60 ? "\u2026" : ""}"` : "Preferences saved."}
      </p>
      ${tagSummary}

      <!-- Edit later link -->
      <div style="margin-top:20px;display:flex;flex-direction:column;gap:8px;">
        <button id="gh-edit-dashboard" style="
          width:100%;padding:10px;border-radius:8px;border:1px solid ${BORDER};
          background:transparent;color:${TEXT};font-size:12px;font-weight:600;
          cursor:pointer;display:flex;align-items:center;justify-content:center;gap:6px;
          font-family:inherit;
        ">
          Edit in dashboard
          <span style="font-size:10px;opacity:0.6;">\u2197</span>
        </button>
        <button id="gh-success-close" style="
          width:100%;padding:10px;border-radius:8px;border:none;
          background:${PURPLE};color:#fff;font-size:13px;font-weight:700;
          cursor:pointer;font-family:inherit;
        ">Done</button>
      </div>

      <!-- Auto-close countdown hint -->
      <p id="gh-countdown" style="margin:10px 0 0;font-size:10px;color:${MUTED};opacity:0.5;">
        Closing in 3 s
      </p>
    </div>
  `;
    document.getElementById("gh-edit-dashboard")?.addEventListener("click", () => {
      if (typeof chrome !== "undefined" && chrome.tabs?.create) {
        chrome.tabs.create({ url: dashboardUrl });
      } else {
        window.open(dashboardUrl, "_blank", "noopener");
      }
      closeSheet();
    });
    document.getElementById("gh-success-close")?.addEventListener("click", closeSheet);
    let remaining = 3;
    const countdownEl = document.getElementById("gh-countdown");
    const ticker = setInterval(() => {
      remaining -= 1;
      if (countdownEl) countdownEl.textContent = `Closing in ${remaining} s`;
      if (remaining <= 0) {
        clearInterval(ticker);
        closeSheet();
      }
    }, 1e3);
    container.addEventListener("click", () => {
      clearInterval(ticker);
      if (countdownEl) countdownEl.style.display = "none";
    }, { once: true });
  }
  function closeSheet() {
    const container = document.getElementById("gh-hint-sheet");
    if (container) container.innerHTML = "";
    if (sheetState?.reject) sheetState.reject(new Error("cancelled"));
    sheetState = null;
  }
  function openHintSheet({
    itemId,
    itemTitle,
    itemRetailer,
    existingHint,
    existingTags,
    wishlistSlug = null
  }) {
    return new Promise((resolve, reject) => {
      let container = document.getElementById("gh-hint-sheet");
      if (!container) {
        container = document.createElement("div");
        container.id = "gh-hint-sheet";
        document.body.appendChild(container);
      }
      sheetState = {
        itemId,
        itemTitle: itemTitle ?? "",
        itemRetailer: itemRetailer ?? "",
        wishlistSlug: wishlistSlug ?? null,
        hint: existingHint ?? "",
        tags: existingTags ? [...existingTags] : [],
        resolve,
        reject
      };
      fetchPopularTags().then(() => renderSheet()).catch(() => renderSheet());
    });
  }
})();
